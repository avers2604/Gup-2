apiVersion: 1

providers:
  - name: Stage 4 HA and DR
    orgId: 1
    folder: Infrastructure
    type: file
    disableDeletion: true
    editable: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/json
