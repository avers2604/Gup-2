-r requirements.txt

# Только для разработки/CI — не тянуть в прод-образ (requirements.txt
# сознательно держится минимальным, см. STACK.md про аудит лицензий).
coverage>=7.6,<8
ruff>=0.9,<1
mypy>=1.14,<2
bandit>=1.8,<2
pip-audit>=2.8,<3
